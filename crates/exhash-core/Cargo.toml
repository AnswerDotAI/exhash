[package]
name = "exhash-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Verified line-addressed file editor using lnhash addresses (core library)"

[lib]
name = "exhash_core"
path = "src/lib.rs"

[dependencies]
regex = "1.7.1"
